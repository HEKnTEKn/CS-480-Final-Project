SELECT id as name, spellblock as magic_resist
FROM championstats
WHERE id = '{1}'; #done

-- returns difficulty of champion
# SELECT id as name, spellblock as magic_resist
# FROM championstats
# WHERE id = 'Swain';